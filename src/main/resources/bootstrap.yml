spring:
  application:
    name: rulemanager
    version: 1.0.0
    
  profiles:
    active: ${spring.profiles.active}
  
  config.import: "optional:configserver:"
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
